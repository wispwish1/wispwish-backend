import { APIResource } from "../core/resource.mjs";
import { RequestOptions } from "../internal/request-options.mjs";
import { APIPromiseWithAwaitableTask } from "../lib/polling.mjs";
export declare class TextToVideo extends APIResource {
    /**
     * This endpoint will start a new task to generate a video from a text prompt.
     */
    create(body: TextToVideoCreateParams, options?: RequestOptions): APIPromiseWithAwaitableTask<TextToVideoCreateResponse>;
}
export interface TextToVideoCreateResponse {
    /**
     * The ID of the newly created task.
     */
    id: string;
}
export interface TextToVideoCreateParams {
    /**
     * Veo 3 videos must be 8 seconds long.
     */
    duration: 8;
    /**
     * The model variant to use.
     */
    model: 'veo3';
    /**
     * A non-empty string up to 1000 characters (measured in UTF-16 code units). This
     * should describe in detail what should appear in the output.
     */
    promptText: string;
    /**
     * A string representing the aspect ratio of the output video.
     */
    ratio: '1280:720' | '720:1280';
    /**
     * If unspecified, a random number is chosen. Varying the seed integer is a way to
     * get different results for the same other request parameters. Using the same seed
     * integer for an identical request will produce similar results.
     */
    seed?: number;
}
export declare namespace TextToVideo {
    export { type TextToVideoCreateResponse as TextToVideoCreateResponse, type TextToVideoCreateParams as TextToVideoCreateParams, };
}
//# sourceMappingURL=text-to-video.d.mts.map